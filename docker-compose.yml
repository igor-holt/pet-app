version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      - GEMINI_API_KEY=AIzaSyBIgbrpqYd9k0IpS0_94tLCYlhoFAEbFxM
    restart: unless-stopped
